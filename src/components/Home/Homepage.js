import React from 'react';
import Header from '../Header';
import Navigation from '../Navigation';

const Homepage = (props) => {
    return (
        <div>
            <h1>eHouseholds Homepage</h1>
        </div>
    )
}

export default Homepage