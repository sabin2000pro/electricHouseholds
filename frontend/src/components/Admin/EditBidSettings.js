import React, {Fragment, useState, useEffect} from 'react';
import { useHistory, useLocation } from 'react-router';

const EditBidSettings = () => {
    let history = useHistory();

    return (
       <Fragment>

       </Fragment>
    )
}

export default EditBidSettings